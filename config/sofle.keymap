#include <dt-bindings/zmk/keys.h>

/ {
    /* Minimal keymap overlay; no encoder bindings to ensure clean build.
       You can add encoder sensor-bindings later once the behavior phandle
       is available (e.g., &inc_dec_kp). */
};

/* RGB helpers can still be bound on a layer in your main keymap:
   &rgb_ug_toggle  &rgb_ug_hue_up  &rgb_ug_hue_down
   &rgb_ug_bri_up  &rgb_ug_bri_down
*/
